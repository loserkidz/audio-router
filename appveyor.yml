image: Visual Studio 2019

configuration: Release

before_build:

build:
  project: audio-router.sln
  verbosity: minimal
  
artifacts:
  - path: build\staging\Audio Router.exe
    name: Audio Router.exe
